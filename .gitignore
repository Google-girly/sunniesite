# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/app/generated/prisma

# local sqlite database
/prisma/dev.db
/prisma/dev.db-journal

# chapter's own private files — passwords, financial books, official
# docs. Never belongs in the repo, gitignored as a backstop even though
# it should live outside this folder entirely.
/Upd. SON Drive 8.8.26/

# a loose working file with a real member's name filled in — not
# referenced by any code, not needed by other chapters.
/event-report-C.1-sample.docx

# local Claude Code settings (machine-specific paths) — not meant to be
# shared/committed.
/.claude/settings.local.json
